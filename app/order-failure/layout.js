export const metadata = {
  title: "Payment Failed - Premika",
  description: "Your payment could not be processed. Please try again or contact support.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function OrderFailureLayout({ children }) {
  return children;
}
